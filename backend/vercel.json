{
    "version": 2,
    "functions": {
      "./server.js": {
        "memory": 1024,
        "maxDuration": 10
      }
    },
    "builds": [
      { "src": "/backend/server.js", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/backend/server.js" }
    ]
  }